# 向参考实现对齐的四项改动（review 用）

分支 `feat/align-dsh-reference`（工作树 `/Users/yin/Documents/ybs/code/pi-webx-align`），
基线 `f98bca0`，共 20 刀。每项都过了 `npm run typecheck` + `npm run check`（11 个脚本）
+ `npm run build`，并在真实浏览器里量过可见部分。

参考物料：你那条需求里带的 8 张截图。`ref0` 是 zcode 的模型编辑器（目标态），
`ref1` 是 pi-webx 改之前的样子，`ref2/ref5` 是你画了红框的现状（工具卡输出区、侧栏
图标列），`ref3` 是 deepseek-harness 的 Bash 终端卡，`ref4` 是侧栏现状。

---

## ① 模型配置编辑器（7 刀）

`ref0` 上逐项对齐：帮助文案从「行内小字」收进 `?` tooltip（7 个）、容量按原始数字
显示（`1000000` / `384000` 占位符同参考）、`高级配置` 折叠、footer 顺序
`重置表单 | 移除模型 | 取消 保存`、输入类型行 `文本 / 图片 / 视频 / PDF`（无音频）、
推理等级的 `✕` 改成 hover 才出现（静止时与参考一样是光名字的 chip）。

| 提交 | 内容 |
|---|---|
| `0dc326e` | 帮助文案移入 `?` tooltip |
| `a494845` | `重置表单` 放回 footer |
| `9eed871` | 容量按原始数字显示 |
| `6cea2d1` | 去掉音频 chip；保存规则提成可测的纯函数 |
| `8fc0711` | `文本` chip 用禁用的勾选框表达必选 |
| `43bc0dc` | 推理等级的 `✕` 改为 hover 出现 |
| `3b9f776` | 输入类型的提示不再提音频 |

**与参考还剩的差别**（都需要你点头才算数）：

1. `文本` chip：ref0 上有个小锁字形，这边是「勾上但点不动的框 + tooltip」。项目
   vendored 的图标集里没有 lock，我没有手搓一个。
2. `模型 ID` / `显示名称` / `移除模型` 保留。ref0 是 zcode「选中模型后编辑」，标识不在
   这个弹窗里；pi-webx 的配置需要它们。
3. 推理等级的词表是 pi 的（`minimal/low/medium/high/xhigh/max`），ref0 是 zcode 的
   （`disabled/low/high/max`）——数据来源不同，不是样式差异。

## ② 工具卡只留结果 + 独立滚动（4 刀）

`bash/powershell/read/write/edit` 展开后只渲染结果：命令在行摘要里，不再叠一块
「命令」；参数不重复渲染；结果区是 150px 的独立滚动容器，caption 在容器**内部**
sticky（对齐 dsh `ToolRow` 的 `.ioSection` / `.ioLabel`）。

| 提交 | 内容 |
|---|---|
| `8e794d7` | 只留结果 + 独立滚动容器 |
| `592655d` | 图片区块移出滚动容器（图是看的东西，不是长日志） |
| `75d0856` | caption 左栏 + sticky |
| `a78070d` | 失败的文件变更显示失败原因 |

浏览器实测：`maxHeight:150px`、`clientHeight:150`、`scrollHeight:4599`；滚动 400px 时
载荷移 `-400px`、caption 移 `0px`；页面上没有 `命令`/`参数` 区块；失败的 edit 只有红色
`输出`、没有 `变更`。`check-tool-card.ts` 把这些钉住（含 `RESULT_ONLY_TOOLS` 里每个名字
必须是 pi 的内建工具名、且行摘要说得清这次调用）。

`a78070d` 这一刀不是对齐，是验收时抓出来的真 bug：`write`/`edit` 的展开体把「变更」和
「输出」做成了二选一，于是这两种调用的结果文本**从来不显示**。失败时最惨——pi 的失败
edit 返回 `isError` + 空 patch（`Could not find edits[14] in …`），卡片却照着参数画出一
份从未落地的 diff，把解释它的错误压在下面。现在失败时变更让位给失败文本。

## ③ 侧栏前导 slot 改成状态点（1 刀）

`ref2/ref5` 的红框指的是这一列：改之前**历史会话全是时钟字形、运行中的反而没有点**。
现在 slot 永远是一个按状态上色的点，优先级
`待用户确认 > 进行中 > 已完成 > 空闲`（对齐 dsh `SessionStatusDots` 的口径：
blocked-on-you 的会话排在一个只是 running 的会话前面）。

`4d96169` 一刀。浏览器实测（本轮，真实触发）：
`warning`（真的弹了一个 ask_question confirm）→ 回答后 `done` → `sleep 20` 期间
`ongoing` → 结束回 `done`；11 行会话 11 个点，slot 里只有点（时钟字形清零）。

## ④ 附件管线（8 刀）

规范化（EXIF 摆正 / 剥元数据与 ICC / 缩进像素预算 / 有 alpha 走 WebP、否则 JPEG，
85-75-60 质量阶梯 / 4MiB 目标）、内容寻址存储（`objects/<sha[0:2]>/<sha>`，`link()`
原子发布，EEXIST 即去重）、`/api/attachments/:id`、消息从存储渲染、重启后仍在、
后续轮次继续发给模型、准入限制（单图字节/张数/单条消息总字节/像素/单边）。

| 提交 | 内容 |
|---|---|
| `1c75de2` | 规范化 |
| `6ced5a7` | 内容寻址存储 + 读取路由 |
| `fea3229` | 消息图片从存储渲染 |
| `0719205` | 移除存储的测试后门 |
| `eee065b` | 总量按解码字节算；`exists()` 不再整读对象 |
| `2ed33ba` | 新建根目录后向上 fsync 一次 |
| `58c5684` | 传输上限装得下准入上限；超限说人话 |
| `57e22b4` | 大图 + EXIF 方向不再被压扁 |

后两刀是**在真实浏览器里验收时抓出来的**：

- **传输把准入架空了** `58c5684`：`express.json` 上限是 `1mb`，而准入写着单图
  20MiB → 超过约 750KB 的图片根本进不了管线，表现是「点了发送，界面一声不吭，草稿
  还在框里」（三处：上限写死、Composer 没接住抛出的失败、413 没有翻译成人话）。
- **大图 + EXIF 方向被压扁** `57e22b4`：缩放框按摆正前的宽高算 + `fit:'fill'`，
  EXIF 5–8 摆正后宽高互换 → 手机竖拍照片横向拉伸一倍。原来那张带方向的测试图太小、
  进不了缩放，所以测不到。

本轮浏览器实测（HEAD）：3.1MB、带 `orientation=6` 的 PNG → 规范化成 515KB WebP
存入存储 → 消息从 `/api/attachments/sha256:924e13a6…` 渲染（inline 0）→ 模型答
「竖的」（产物 1704×2461，修之前是 2461×1704）；同一张 1600×1200 的图连发两轮，
第二**不带附件**问「红色方块在左上/右上/左下/右下」，答「左上」；重启桥接后新进程
仍能服务该对象、页面照常从存储渲染；21MB 的图由管线自己回「图片超过单张字节上限。」；
重复发同一文件存储对象数不变。

---

## 还没做的两件事

1. **上 main**。main 工作区现在有另一个会话的 32 个未提交文件，其中 **9 个**与这 20 刀
   重叠：`server/pi/host.ts`、`server/routes.ts`、`src/components/Composer.tsx`，以及
   4 个 `src/components/sidebar/*`（里面还夹着我早先留在主树的半份 ③）。现在合并必然
   打架，得先定顺序。
2. **①那个小锁**：要不要我手绘一个，还是就这样。

## 环境观察（不是产品问题，但会误导验收）

桥接刚重启后的**第一次**打开某个会话页，可能显示「会话已结束 / 该会话已不在服务端
运行」；此时 `POST /api/sessions {sessionId}` 直接打桥接是 **200**，刷新一次页面就正常。
看起来是 dev 下 Vite 代理复用了指向已死进程的 keep-alive 连接（生产是 express 同进程
托管，没有这一跳）。
